<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}Erreur {{ status_code }}{% endblock %}</title>

    {% block stylesheets %}
        <link rel="stylesheet" href="{{ asset('styles/app.css') }}">
    {% endblock %}
</head>

<body class="hold-transition lockscreen">

    <div class="lockscreen-wrapper">
        <div class="lockscreen-logo">
            <a href="#"><b>{% block content_title %}{% endblock %}</b></a>
        </div>

        <section class="content">
            <div class="error-page">
                <h2 class="headline text-danger">{{ status_code }}</h2>

                <div class="error-content">
                    <h3><i class="fas fa-exclamation-triangle text-danger"></i> Oops! {{ status_text }}</h3>
                    <p>Une erreur est survenue, mais nous travaillons à sa résolution.</p>
                    <p>Néanmoins, vous pouvez <a href="/">retourner à l’accueil</a>.</p>
                </div>
            </div>
        </section>
    
        <div class="lockscreen-footer text-center mt-5">
        {% block copyright %}
            Copyright &copy; {{ "now"|date('Y') }} <b><a href="https://github.com/sabinus52/OlixBackOfficeBundle" class="text-black">OlixBackOfficeBundle</a></b><br>
            All rights reserved
        {% endblock %}
        </div>
    </div>


    {% block javascripts %}
        {% block importmap %}{{ importmap('app') }}{% endblock %}
    {% endblock %}
</body>
</html>
